<!DOCTYPE html>
<html lang="en">
{% include partials/head.html %}
<body class="bg-gradient-to-r from-gray-100 via-gray-50 to-gray-100">
  <div class="flex flex-col min-h-screen">

    {% if page_title == "Home" %}
      <header>
        <img src="../img/about-01.png" alt="" class="object-cover h-64 min-w-full md:h-auto">
      </header>
    {% endif %}
    
    <main class="flex{% if page_title == "Home" %} flex-col{% endif %} flex-grow md:flex-row">
      {% include partials/nav.html %}
      {{ content | safe }}
    </main>
  </div>
</body>
</html>
